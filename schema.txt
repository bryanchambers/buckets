CREATE TABLE user(id INTEGER PRIMARY KEY, pod_id INTEGER, username TEXT, password TEXT);

CREATE TABLE pod(id INTEGER PRIMARY KEY, name TEXT, refill INTEGER);

CREATE TABLE bucket(id INTEGER PRIMARY KEY, pod_id INTEGER, name TEXT, balance INTEGER, refill INTEGER, size INTEGER);

CREATE TABLE purchase(id INTEGER PRIMARY KEY, bucket_id INTEGER, desc TEXT, amount INTEGER, date INTEGER);